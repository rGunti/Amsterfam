/**
 * The one locale all dates and times are shown in: 24h clock, day before month. Used for
 * Angular's pipes (LOCALE_ID) and for direct Intl formatting, so both agree.
 */
export const APP_LOCALE = 'en-GB';
